<!DOCTYPE html>
<html lag="ja">
    <head>
    </head>
    <body>
        <script>
            while (true) {
            var a = prompt("コードを入力して下さい","");
            var b = prompt("コードを入力して下さい","");
            var c = prompt("コードを入力して下さい","");
            var d = b+0+0+0+0;
            var e = 0+b;
            var f = a+d+e;
            alert("コードは" + f);
            }
 
            
            </script>
        </body>
    </html>
